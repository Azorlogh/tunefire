// use url::Url;

// use crate::MyServer;

// impl MyServer {
// 	pub fn get_track_info(&self, url: &Url) -> Result<(), Box<dyn std::error::Error>> {
// 		let track_info = self
// 			.plugins
// 			.iter()
// 			.filter_map(|p| p.handle_url(url))
// 			.first();
// 		Ok(())
// 	}
// }
